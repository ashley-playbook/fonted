{
  "name": "fonted",
  "private": true,
  "description": "Fonted repo root — build runs in glyph/",
  "scripts": {
    "build": "npm install --prefix glyph && node glyph/build.js",
    "deploy": "npm run build && npm exec --prefix glyph -- wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
