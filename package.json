{
  "name": "fonted",
  "private": true,
  "description": "Fonted repo root — build runs in glyph/",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && npx wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
