{
  "name": "fonted",
  "private": true,
  "description": "Fonted — fancy text & username generator (static site)",
  "scripts": {
    "build": "node build.js",
    "validate": "node build.js --validate",
    "generate": "node scripts/generate-pages.js",
    "generate:force": "node scripts/generate-pages.js --force",
    "ship": "node scripts/generate-pages.js --force && node build.js --validate && node build.js",
    "deploy": "node build.js && wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.24.4"
  }
}
